@model List<Announcement>;

<div class="text-center">
    <div class="card mb-3 h-100">
        @foreach (var i in Model)
        {
            <partial name="_AnnouncementCardPartial" model="@i" />
        }
    </div>
</div>
